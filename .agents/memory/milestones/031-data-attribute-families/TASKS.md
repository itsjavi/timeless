# Milestone 031 Tasks

## 1. Declare the per-item family

- [x] Add a fifth positional `attributes` field to `part()` in `component-registry.mjs`
- [x] Declare `data-ui-value` and `data-ui-label` on the `option` part in `COLLECTION_PARTS`
- [x] Declare `data-ui-value` on the `tab` part, and name the attribute in the `chip-remove`
      description that already describes the library writing it
- [x] Add `attributes` to `ComponentPartContract` and emit it from `generate-elements.mjs`, into
      both `contracts.ts` and `custom-elements.json`'s `timeless:parts`
- [x] Surface part attributes on the reference page (`[slug].astro`) and in the `.md` output
      (`component-markdown.ts`)
- [x] Surface them in the packaged skill (`emit-agent-skill.mjs`)
- [x] Assert the new part shape in `validateContractShape` in `validate-contracts.mjs`
- [x] Replace the hardcoded `name === 'data-ui-value'` literal with a set derived from declared part
      attributes
- [x] Teach `check-markup.mjs` to check `data-ui-*` on an element carrying `data-ui-part`, reporting
      `undeclared-attribute` for a name no matching part declares
- [x] Cover the new finding in `check-markup.test.mjs`

## 2. Retire two private hooks

- [x] Remove `data-ui-internal-placement` from `floating.ts` and its assertions in
      `floating.test.ts`
- [x] Replace `data-ui-internal-paged` with a `WeakSet` in `options.ts`
- [x] Confirm no occurrence of either name survives outside milestone records

## 3. Write the families down

- [x] Add the four-family table to `AGENTS.md`
- [x] Update the `data-ui-internal-*` prose in `AGENTS.md` and the packaged skill if the removals
      contradict it

## 4. Gate

- [x] `pnpm generate` clean, then `contracts:validate`, `manifest:validate`, `core:validate`,
      `exports:validate`
- [x] `pnpm test`
- [x] `pnpm qa`
- [x] `audit-component-contracts` over the diff
- [x] Write `RESULTS.md`

---

Generated by Claude Opus 5 (High)
